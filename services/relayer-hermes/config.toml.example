# Hermes IBC Relayer — config.toml de ejemplo
# Copiar a persistent-data/config.toml y editar con tus cadenas y endpoints.
# Ver README.md, sección "Campos de config.toml", para qué es cada campo y de dónde obtener los valores.

[global]
log_level = 'info'

# ——— Cadena A ———
# id = Chain ID oficial de la red (ej. cosmoshub-4, osmosis-1), no un alias local
# account_prefix = Prefijo bech32 de direcciones en esta cadena (cosmos, osmo, infinite...)
# store_prefix = Casi siempre "ibc" en cadenas Cosmos SDK
[[chains]]
id = 'chain-a'
rpc_addr = 'http://localhost:26657'
grpc_addr = 'http://localhost:9090'
account_prefix = 'cosmos'
key_name = 'relayer-chain-a'
store_prefix = 'ibc'

# ——— Cadena B ———
[[chains]]
id = 'chain-b'
rpc_addr = 'http://localhost:26667'
grpc_addr = 'http://localhost:9090'
account_prefix = 'cosmos'
key_name = 'relayer-chain-b'
store_prefix = 'ibc'
